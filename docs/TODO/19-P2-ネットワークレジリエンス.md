# 19: P2 ネットワークレジリエンス

**優先度**: 🟡 P2（中期対応）
**関連レポート**: [DEBUG-ANALYSIS-REPORT.md](../architecture/DEBUG-ANALYSIS-REPORT.md) §4, §7
**見積工数**: 合計 6.5h
**ワークストリーム**: Stream D（Stream A 完了後推奨）

---

## 概要

イベント中のネットワーク不安定性・セッション切断に対するレジリエンスを強化する。
Vercel ランタイム制約への対応も含む。

---

## タスク一覧

### Task D1: `fetchWithRetry` ユーティリティ作成（2h）

**ファイル**: `src/lib/fetch-with-retry.ts` (新規作成)
**依存**: なし

```typescript
interface FetchWithRetryOptions extends RequestInit {
    retries?: number
    retryDelay?: number  // ms
    onAuthError?: () => void
}

export async function fetchWithRetry(
    url: string,
    options: FetchWithRetryOptions = {}
): Promise<Response> {
    const { retries = 3, retryDelay = 1000, onAuthError, ...fetchOptions } = options
    
    for (let attempt = 0; attempt <= retries; attempt++) {
        try {
            const res = await fetch(url, fetchOptions)
            
            if (res.status === 401) {
                onAuthError?.()
                throw new Error('Session expired')
            }
            
            if (res.ok || res.status < 500) {
                return res
            }
            
            // 5xx エラーはリトライ
            if (attempt < retries) {
                const delay = retryDelay * Math.pow(2, attempt) // exponential backoff
                await new Promise(r => setTimeout(r, delay))
                continue
            }
            
            return res
        } catch (error) {
            if (attempt < retries && !(error instanceof Error && error.message === 'Session expired')) {
                const delay = retryDelay * Math.pow(2, attempt)
                await new Promise(r => setTimeout(r, delay))
                continue
            }
            throw error
        }
    }
    
    throw new Error('Max retries exceeded')
}
```

- [ ] Exponential backoff 実装
- [ ] 401 エラーの特別ハンドリング
- [ ] ネットワークエラー時のリトライ
- [ ] 5xx のみリトライ（4xx はリトライしない）

### Task D2: 401エラー → ログインリダイレクト（1h）

**ファイル**: `src/app/staff/diagnosis/[id]/page.tsx`
**依存**: D1

```typescript
// 診断ページ内の全 fetch 呼び出しを fetchWithRetry に置き換え
import { fetchWithRetry } from '@/lib/fetch-with-retry'

const handleAuthError = useCallback(() => {
    router.push('/staff/login?redirect=' + encodeURIComponent(window.location.pathname))
}, [router])

// 使用例
const res = await fetchWithRetry('/api/staff/session', {
    onAuthError: handleAuthError,
})
```

### Task D3: `online` イベントで自動再取得（1h）

**ファイル**: `src/app/staff/diagnosis/[id]/page.tsx`
**依存**: なし

```typescript
useEffect(() => {
    const handleOnline = () => {
        if (visitError) {
            // ネットワーク復帰時に自動再取得
            setVisitError(null)
            setIsLoadingVisit(true)
            fetchVisitData()
        }
    }
    
    const handleOffline = () => {
        // オフライン時は保存を即時実行
        flushToStorage()
    }
    
    window.addEventListener('online', handleOnline)
    window.addEventListener('offline', handleOffline)
    return () => {
        window.removeEventListener('online', handleOnline)
        window.removeEventListener('offline', handleOffline)
    }
}, [visitError, flushToStorage])
```

### Task D4: `maxDuration` 設定追加（0.5h）

**ファイル**: 各API route
**依存**: なし（独立作業）

```typescript
// /api/ai/generate-report/route.ts
export const maxDuration = 30  // AI分析は30秒まで

// /api/diagnosis/complete/route.ts
export const maxDuration = 15  // LINE送信を含むため15秒

// /api/line/send-report/route.ts
export const maxDuration = 15
```

### Task D5: ネットワーク状態インジケーター（1h）

**ファイル**: `src/components/NetworkStatusIndicator.tsx` (新規作成)
**依存**: なし

```typescript
'use client'
import { useEffect, useState } from 'react'

export function NetworkStatusIndicator() {
    const [isOnline, setIsOnline] = useState(true)
    
    useEffect(() => {
        const handleOnline = () => setIsOnline(true)
        const handleOffline = () => setIsOnline(false)
        
        window.addEventListener('online', handleOnline)
        window.addEventListener('offline', handleOffline)
        setIsOnline(navigator.onLine)
        
        return () => {
            window.removeEventListener('online', handleOnline)
            window.removeEventListener('offline', handleOffline)
        }
    }, [])
    
    if (isOnline) return null
    
    return (
        <div className="fixed top-0 left-0 right-0 z-50 bg-red-500 text-white text-center py-2 text-sm font-medium">
            ⚠️ オフラインです。ネットワーク接続を確認してください。
        </div>
    )
}
```

### Task D6: AI分析の非同期化設計（1h）

**将来的な改善** — ドキュメント作成のみ

```
現在: /api/ai/generate-report → 同期的に待機（タイムアウトリスク）
将来: 
  Step 1: /api/ai/generate-report → ジョブID返却（即座に応答）
  Step 2: フロントがポーリングで完了を待つ
  Step 3: 完了後にレポート取得
```

---

## 完了条件

- [ ] D1: `fetchWithRetry` ユーティリティ作成
- [ ] D2: 診断ページの全fetchを置き換え
- [ ] D3: online/offline イベントハンドリング
- [ ] D4: `maxDuration` 設定
- [ ] D5: ネットワーク状態UI表示
- [ ] D6: 非同期化設計ドキュメント作成
